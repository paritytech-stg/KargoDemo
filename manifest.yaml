images:
  frontend:
    dockerfile: KargoDemo/Dockerfile
    context: .
    port: 80
    ingressRoute: /
  backend:
    dockerfile: KargoDemoServer/Dockerfile
    context: .
    port: 5000
    ingressRoute: /api